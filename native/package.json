{
  "name": "xpro-native",
  "version": "0.1.0",
  "private": true,
  "napi": {
    "name": "xpro-native",
    "triples": {
      "defaults": true
    }
  },
  "scripts": {
    "build": "napi build --release --platform",
    "build:debug": "napi build --platform"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.0.0"
  }
}
